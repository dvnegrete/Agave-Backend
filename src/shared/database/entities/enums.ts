export enum Role {
  ADMIN = 'admin',
  OWNER = 'owner',
  TENANT = 'tenant',
}

export enum Status {
  ACTIVE = 'active',
  SUSPEND = 'suspend',
  INACTIVE = 'inactive',
}

export enum ValidationStatus {
  NOT_FOUND = 'not-found',
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
}
